# Project Structure

Use this file when organizing Python modules and scripts.

- Keep reusable logic in modules rather than scripts.
- Keep CLI or task scripts thin wrappers around reusable module code.
- Add docstrings when behavior is not obvious from naming and types.

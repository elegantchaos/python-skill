# Environments and Validation

Use this file when changing dependencies, environment setup, or validation.

- Use the project's standard environment and tooling approach.
- Avoid adding dependencies without clear justification.
- Follow existing version pinning or constraint conventions.
- Run relevant tests, lint checks, and type checks for changed code.
- If checks cannot run, report the exact gap and potential impact.
